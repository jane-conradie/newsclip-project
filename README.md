# newsclip-project
Software Development Test

The changeable database connection string is found in: controllers/db_connection.php.

The index.php file should be opened first so that the stored procedure that pre-populates the data can be called.
